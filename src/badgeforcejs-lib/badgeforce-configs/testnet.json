{
    "testnet": {
        "base": "https://testnet.badgeforce.io/alpha",
        "routes": {
            "state": "/state", 
            "ipfs": "/ipfs", 
            "batches": {
                "submit": "/batches",
                "statuses": "/batch_statuses"
            }
        }
    }, 
    "development": {
        "base": "http://localhost:8008",
        "routes": {
            "state": "/state", 
            "ipfs": "/ipfs", 
            "batches": {
                "submit": "/batches",
                "statuses": "/batch_statuses"
            }
        }
    }
}